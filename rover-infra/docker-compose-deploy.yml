version: '3'

services:
  wifiap:
    image: docker.ie.khondar.name/rover/wifiap:${TAG}
    restart: always
    network_mode: host
    privileged: true

#  servoblaster:
#    image: docker.ie.khondar.name/servoblaster:latest
#    restart: always
#    privileged: true

  rproxy:
    image: docker.ie.khondar.name/rover/rproxy:${TAG}
    ports:
      - 8080:80
    restart: always

# end